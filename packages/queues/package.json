{
  "name": "@devent/queues",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for @devent/queues\""
  },
  "dependencies": {
    "@devent/events": "*",
    "@devent/utils": "*",
    "bullmq": "^5.8.1",
    "ioredis": "^5.4.1"
  }
}
