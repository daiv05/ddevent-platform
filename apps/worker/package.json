{
  "name": "worker",
  "version": "1.0.0",
  "description": "devent worker",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for worker\""
  },
  "dependencies": {
    "@devent/database": "*",
    "@devent/queues": "*",
    "@devent/utils": "*",
    "@devent/workflows": "*",
    "bullmq": "^5.8.1"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
